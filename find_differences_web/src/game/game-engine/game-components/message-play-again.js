import ContainerBase from "../../../components/container-base.js";

export class MessagePlayAgain extends ContainerBase {
    constructor(stage, descriptor) {
        super(stage, descriptor);
    }
}